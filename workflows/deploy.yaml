name: Deploy to Google Cloud Storage

on:
  push:
    branches:
      - master # Change to your preferred branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    # Checkout the repository code
    - name: Checkout code
      uses: actions/checkout@v2

    # Set up Google Cloud SDK
    - name: Set up Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: level-scheme-451514-m8  # Replace with your Google Cloud project ID
        service_account_key: ${{ secrets.GCP_SA_KEY }}
        service_account_email: ${{ secrets.GCP_SA_EMAIL }}  # Service Account JSON secret

    # Sync files to Google Cloud Storage
    - name: Deploy to Google Cloud Storage
      run: |
        gsutil -m cp -r * gs://sarahportfolio.cloud/
    
    - name: List Files Before Deployment
      run: ls -R

    - name: Deploy to Google Cloud Storage
      run: |
        gsutil -m rsync -R ./public gs://sarahportfolio.cloud/


